#include <iostream>
#include <string>
#include "Capybara.h"
#include "Wagon.h"

int main(){
    Wagon wagon;
    wagon.addCapybara(Capybara c1);
    wagon.addCapybara(Capybara c2);
    wagon.addCapybara(Capybara c3);
    wagon.addCapybara(Capybara c4);
    wagon.addCapybara(Capybara c5);
    return 0;
}