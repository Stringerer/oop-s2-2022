#include <iostream>
#include <string>
#include "Capybara.h"
#include "Wagon.h"

int main(){
    Wagon wagon;
    Capybara c1;
    wagon.addCapybara(c1);
    Capybara c2;
    wagon.addCapybara(c2);
    Capybara c3;
    wagon.addCapybara(c3);
    Capybara c4;
    wagon.addCapybara(c4);
    Capybara c5;
    wagon.addCapybara(c5);
    return 0;
}